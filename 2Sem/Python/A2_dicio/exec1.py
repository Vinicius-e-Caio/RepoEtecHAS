def verifyThis(name: str, age: int) -> None:
    for keys in contato.keys():
        if keys.lower() == name.lower():
            found = True
    if found:
        newNome = str(input("Digite: "))
        newIdade = int(input("Digite idade"))    
    else:
        return None


# 
# tabela = []
# nome = str(input("Digite: "))
# idade = int(input("Digite idade"))
# tabela.append(contato.copy())

contato = {
    'nome'
    'idade'
}
tabela = []
while True:
    tabela.append(contato.copy())
    nome = verifyThis(input("Nome: "))
    if nome == None:
        idade = int(input("Idade: "))
        tabela.append(contato.copy())
    else:
        for i in range:
        
    